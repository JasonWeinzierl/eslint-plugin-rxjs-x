import { ESLintUtils } from "@typescript-eslint/experimental-utils";

const { getParserServices } = ESLintUtils;
export {
  /**
   * @deprecated Use `ESLintUtils.getParserServices` directly.
   */
  getParserServices,
};
